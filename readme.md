# 项目注意事项

1. **禁用 `<select>` 标签** - 手机钱包不兼容
2. **CSS 不能直接写在 Vue 文件中** - 需要单独开文件，然后导入到 Vue 文件内
3. **模拟资料** - 按 Vue Options 风格编写
4. **JS 特效** - 单独开一个 TypeScript 文件，然后导入到 Vue 文件内
5. **HTML 必须闭合** - 所有 HTML 标签必须正确闭合
